import React from 'react';
import {Navbar} from './Navbar'

export default {
    title: 'Navbar',
    component: Navbar,
}

export const Template = () => <Navbar>Navbar</Navbar>

export const Main = Template.bind({});